fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.5.0
psycopg[binary]>=3.2.0
cryptography>=42.0.0
databricks-sdk>=0.40.0
openai>=1.52.0
requests>=2.31.0
# Async HTTP client for the edge / token-broker reverse proxy (edge/).
httpx>=0.27.0
# Managed MCP transport (Genie / UC functions / SQL servers). Optional at
# runtime — only imported when MT_GENIE_TRANSPORT=mcp.
mcp>=1.9
databricks-mcp>=0.9.0
